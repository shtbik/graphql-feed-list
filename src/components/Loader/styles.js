const styles = theme => ({
	loaderBlock: {
		position: 'absolute',
		height: '100%',
		width: '100%',
		display: 'flex',
		alignItems: 'center',
	},
	progress: {
		margin: `${theme.spacing(2)}px auto`,
		display: 'block',
	},
})

export default styles
